package tokenbucket;

/**
 * @author wuhao
 */
public class DefaultTokenAddHandle implements TokenAddHandle {


//    long newTokens = Math.min(getCapacity(), Math.max(0, refillStrategy.refill()));
//    setSize(Math.max(0, Math.min(getSize() + newTokens, getCapacity())));
    @Override
    public void add() {

    }

}
